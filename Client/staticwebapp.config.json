{
    "navigationFallback": {
        "rewrite": "/index.html"
    },
    "routes": [
        {
            "route": "/api/*",
            "allowedRoles": ["anonymous"]
        },
        {
            "route": "/version.json",
            "headers": {
                "Cache-Control": "no-store"
            }
        }
    ]
}